/* pages/todo/index.wxss */
.container {
  padding: 16rpx;
  padding-bottom: 120rpx;
  background: #f5f5f5;
  min-height: 100vh;
}

/* 统计卡片 */
.stats-card {
  display: flex;
  background: linear-gradient(135deg, #FF85A2 0%, #FF6B8A 100%);
  border-radius: 12rpx;
  padding: 16rpx 12rpx;
  margin-bottom: 16rpx;
}

.stat-item {
  flex: 1;
  text-align: center;
}

.stat-num {
  font-size: 32rpx;
  font-weight: bold;
  color: #fff;
}

.stat-label {
  font-size: 22rpx;
  color: rgba(255,255,255,0.8);
  margin-top: 4rpx;
}

/* Tab切换 */
.tabs {
  display: flex;
  background: #fff;
  border-radius: 12rpx;
  padding: 8rpx;
  margin-bottom: 16rpx;
}

.tab {
  flex: 1;
  text-align: center;
  padding: 12rpx 0;
  font-size: 26rpx;
  color: #666;
  border-radius: 8rpx;
}

.tab.active {
  background: #FF85A2;
  color: #fff;
}

/* 待办卡片 - 单行紧凑布局 */
.todo-list {
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.todo-card {
  display: flex;
  align-items: center;
  background: #fff;
  border-radius: 10rpx;
  padding: 14rpx 16rpx;
  position: relative;
}

.todo-dot {
  width: 8rpx;
  height: 8rpx;
  border-radius: 50%;
  margin-right: 12rpx;
  flex-shrink: 0;
}

.todo-dot.pending { background: #faad14; }
.todo-dot.doing { background: #1890ff; }
.todo-dot.done { background: #52c41a; }

.todo-content {
  flex: 1;
  min-width: 0;
  overflow: hidden;
}

.todo-row {
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.priority-icon {
  font-size: 26rpx;
  flex-shrink: 0;
}

.todo-title {
  font-size: 28rpx;
  color: #333;
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.todo-title.done {
  color: #999;
  text-decoration: line-through;
}

.mini-avatar {
  width: 24rpx;
  height: 24rpx;
  border-radius: 50%;
  flex-shrink: 0;
}

.todo-time {
  font-size: 22rpx;
  color: #999;
  flex-shrink: 0;
  margin-left: 4rpx;
}

.todo-actions {
  flex-shrink: 0;
  margin-left: 8rpx;
}

.action-btn.delete {
  width: 32rpx;
  height: 32rpx;
  border-radius: 6rpx;
  background: #fff1f0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20rpx;
  color: #f5222d;
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40rpx;
  background: #fff;
  border-radius: 10rpx;
}

.empty-icon {
  font-size: 48rpx;
  margin-bottom: 8rpx;
}

.empty-text {
  font-size: 26rpx;
  color: #999;
}

/* 底部操作栏 */
.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  justify-content: space-between;
  padding: 12rpx 16rpx;
  background: #fff;
  box-shadow: 0 -2rpx 6rpx rgba(0,0,0,0.05);
}

.btn-clear {
  padding: 14rpx 24rpx;
  background: #f5f5f5;
  color: #666;
  border-radius: 8rpx;
  font-size: 26rpx;
}

.btn-add {
  padding: 14rpx 24rpx;
  background: #FF85A2;
  color: #fff;
  border-radius: 8rpx;
  font-size: 26rpx;
}

/* 弹窗 */
.modal-mask {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 999;
}

.modal-content {
  width: 85%;
  background: #fff;
  border-radius: 12rpx;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx 16rpx;
  background: #FF85A2;
  color: #fff;
  font-size: 30rpx;
}

.modal-close {
  font-size: 36rpx;
  padding: 4rpx;
}

.modal-body {
  padding: 20rpx 16rpx;
}

.form-item {
  margin-bottom: 16rpx;
}

.form-label {
  font-size: 26rpx;
  color: #333;
  margin-bottom: 8rpx;
  display: block;
}

.form-input {
  width: 100%;
  height: 64rpx;
  padding: 0 12rpx;
  border: 1rpx solid #ddd;
  border-radius: 8rpx;
  font-size: 26rpx;
  box-sizing: border-box;
}

.priority-btns {
  display: flex;
  gap: 10rpx;
}

.priority-btn {
  flex: 1;
  height: 56rpx;
  background: #f5f5f5;
  border-radius: 8rpx;
  font-size: 24rpx;
  color: #666;
  display: flex;
  align-items: center;
  justify-content: center;
}

.priority-btn.active {
  background: #FF85A2;
  color: #fff;
}

.priority-btn.important.active { background: #fa8c16; }
.priority-btn.urgent.active { background: #f5222d; }

.picker-value {
  height: 64rpx;
  padding: 0 12rpx;
  border: 1rpx solid #ddd;
  border-radius: 8rpx;
  font-size: 26rpx;
  color: #333;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.picker-arrow {
  font-size: 16rpx;
  color: #999;
}

.modal-footer {
  display: flex;
  padding: 16rpx;
  gap: 12rpx;
}

.btn-cancel {
  flex: 1;
  height: 64rpx;
  background: #f5f5f5;
  color: #666;
  border-radius: 8rpx;
  font-size: 26rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.btn-confirm {
  flex: 1;
  height: 64rpx;
  background: #FF85A2;
  color: #fff;
  border-radius: 8rpx;
  font-size: 26rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}
